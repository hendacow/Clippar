# Security audit — 2026-08-20

Daily audit, started with zero context. Branch `security/2026-08-20`, cut from
`origin/main` @ f7b6948.

**Bottom line: one live credential is exposed in pushed git history and must be
rotated by hand now** (item 1a). One further finding was surfaced in review and
confirmed: the bundled `EXPO_PUBLIC_PIPELINE_API_KEY` is the sole auth for the
`app.py` GPU-spend endpoints, so it protects nothing if that Render service is
deployed (§2). Beyond those two, no new exploitable vulnerability was found in
code: the two prior audits (`SECURITY_AUDIT_2026-07-29.md`,
`TENANT_ISOLATION_2026-07-31.md`) closed the serious findings, and I verified
those fixes are still present and intact in HEAD. A second thing that matters is
a *deployment* question, not a code one: **migration 019 — which closes a P0
"free lifetime Pro" hole — has no record of ever being applied to the live
Supabase project.** If it hasn't been, the P0 is live right now.

> **Scope note / how the credential was nearly missed:** the repo arrived as a
> shallow clone (50 commits, back to 2026-07-23). My first secret sweep ran only
> against that truncated history and reported clean — wrongly. A `git fetch
> --unshallow` pulled the full 454-commit history and all remote branches, and
> the credential below lives on an **unmerged remote branch** that a
> checked-out-ref scan (including the repo's own CI `secret-scan.sh`) never
> sees. Every finding here was re-run against full history.

No code was changed on this branch. Every clear-cut fix was already in the tree;
the remaining items either can't be fixed blind (they need prod state I can't
see) or are authorization-path changes I won't make unattended at the risk of
locking out a legitimate admin. This report is the deliverable.

---

## 1. Henry — act by hand (cannot wait, cannot be done from here)

### 1a. CRITICAL — rotate the exposed Resend API key now (live, in pushed history)
`Mailing key.docx` (binary, 13,545 bytes) was committed in `5aa2835`
("pre-repair backup", 2026-08-09, author hendacow). That commit is **not on
`main`**, but it is the tip of remote branch **`origin/feat/always-on-trim-preview`**,
so it is pushed to `github.com/hendacow/Clippar` and fetchable by anyone with
repo access. Verified today: the branch ref resolves to `5aa2835`, the blob is
present, and its extracted XML contains two credentials — a **Resend API key**
(correct `re_` shape, not a placeholder) and a second 32-char token labeled
"SMTP KEY SENDER". No key material (not even prefixes) is reproduced in this
report — it is itself committed and the key is not yet confirmed rotated; the
identifying prefixes were sent to Henry in the private task notification, and the
full values are in the docx on that branch if you need to confirm which to
revoke.

I could not network-test whether they are still live (the proxy blocks
outbound), so they must be treated as live and compromised.

> **Attacker:** anyone who can read the repo (or clone the pushed branch).
> **They send:** requests to the Resend API with this key.
> **They get:** the ability to send email as Clippar's verified sending domain —
> phishing golfers with lookalike "password reset" / "your reel is ready"
> mail — plus burning the sending quota and reading account/suppression data
> through the Resend API.

The July 23 audit already flagged this doc as needing rotation; it was **never
rotated or purged**, and it is now in pushed history. `.gitignore` does cover
`Mailing key.docx`, but that rule postdates this commit, so it does nothing for
the already-committed blob on this branch.

**Do this, by hand (I did not touch git history, per audit rules):**
1. **Rotate the Resend API key immediately** at resend.com → API Keys — revoke
   the key in that docx and issue a new one. Treat the second "SMTP KEY SENDER"
   token as compromised too and rotate whatever it authenticates (the
   Sender.net/SMTP path).
2. **Purge the blob from GitHub:** delete remote branch
   `feat/always-on-trim-preview` if its work is merged/abandoned, or rewrite it
   to drop `Mailing key.docx` and `clippar_app/kickbacks.vsix` (item 1d) and
   force-push. Do not do this against a branch someone is still building on
   without coordinating.
3. **Extend the CI secret-scan** to sweep *all* remote branches, not just the
   pushed ref — `scripts/secret-scan.sh` runs on the checked-out ref only, which
   is exactly why this hid.

### 1b. Confirm migration 019 is actually applied to production — gates a P0
`clippar_app/supabase/migrations/019_tenant_isolation.sql` is committed to `main`
(via PR #120, commit `59d1bf1`) and is correct. But the only written deployment
record we have — the "DEPLOYED — 29 July 2026" section of the July 29 audit —
predates 019 and covers only migrations 016 and 017. **Nothing anywhere confirms
019 ran against the live Supabase project.** I have no DB access, so I cannot
check.

This is not a paperwork nit. Until 019 §1 is applied, this is live (attack
stated in prose rather than as a copy-paste payload, since this report is
committed and the fix is unconfirmed — the full mechanics are in
`TENANT_ISOLATION_2026-07-31.md` finding 1):

> **Attacker:** any signed-up user with the shipped anon key and a valid JWT for
> their own account.
> **What they do:** write their own `profiles` row through the REST API with the
> subscription columns set to an active, never-expiring state — reachable
> whenever that row is briefly absent (a swallowed `handle_new_user` insert, or a
> `delete-account` that removed the profile row then failed before deleting the
> auth user, leaving a live session).
> **They get:** permanent lifetime Pro (an active status with null expiry is read
> as lifetime by `lib/subscription.ts:110` and `has_active_pro()`), with no
> purchase and no RevenueCat event — invisible to the webhook that is supposed to
> be authoritative.

019 §2 (shots round-ownership), §4 (clips storage policies) and §4d (force
`clips` bucket private) are gated on the same deploy.

**Do this:** run `supabase migration list` (or check the `supabase_migrations`
table) against prod. If 019 is not there, apply it to **dev first**, then prod,
exactly as 016/017 were. Then re-run the profile-INSERT probe above with your own
JWT and confirm it is rejected.

### 1c. Verify the `avatars` storage bucket's policies — carried over from 2026-07-31, still open
This was finding #10 of the tenant-isolation audit and it was **never resolved** —
migration 019 §6 only *prints* the bucket policies at deploy time; it deliberately
does not change them, because multiple permissive storage policies are OR'd and a
new one can only widen access. So the fix requires reading prod and tightening by
hand, which has not happened.

> **Attacker:** any signed-in user. (Attack in prose, not a copy-paste call,
> since the bucket fix is unconfirmed and this report is committed.)
> **What they do:** upload to the `avatars` bucket at another user's predictable
> key path, using the same overwrite flag the app itself uses.
> **They get:** if the `avatars` bucket has a bucket-wide INSERT/UPDATE policy
> (just `bucket_id = 'avatars'` with no owner join), they overwrite any other
> user's avatar with content of their choosing. The key is predictable from the
> user id and the app uploads with overwrite enabled (`app/profile/edit.tsx:96`),
> read back via `getPublicUrl` — so the planted image is served as the victim's
> avatar.

**Do this:** run the enumeration query from `TENANT_ISOLATION_2026-07-31.md`
(finding 10) — or read the `[019]` NOTICE lines from the 019 deploy log — for the
`avatars`, `music` and `reels` buckets. For any policy whose expression is just
`bucket_id = '<name>'` with no `(storage.foldername(name))[1] = auth.uid()::text`
owner check, replace it with an owner-scoped one. `avatars` is the one that
matters; `music`/`reels` are lower risk but should be confirmed read-only-or-owned.

### 1d. Confirm the leaked GolfCourseAPI key rotation stuck, and purge two binaries (informational)
The July 29 audit reports the GolfCourseAPI key (`JL2HSOEKW7QQ6MH2ZYQUIZLNRM`,
committed in the `.env.*.example` files) was rotated and the new key confirmed
different, and I verified the code no longer ships it — `lib/golfCourseApi.ts`
now goes through the `search-courses` edge function and holds no credential. The
**old** key remains in git history permanently (reachable from `main`) and must
stay dead. If the rotation's completeness is in any doubt, rotate again at
golfcourseapi.com. Not fixable from code (vendor-side rotation).

Also on branch `feat/always-on-trim-preview` @ `5aa2835`, alongside the docx:
`clippar_app/kickbacks.vsix` (288 KB, a packaged third-party VS Code extension).
Unpacked and scanned — it contains **no Clippar credential** (only its own
vendor backend URLs), so it is not a secret leak, just an unwanted committed
binary the July audit already asked to delete. Purge it in the same history
rewrite as 1a step 2.

---

## 2. Findings by severity

### Medium (High if the Render pipeline service is deployed) — bundled `EXPO_PUBLIC_PIPELINE_API_KEY` is the sole auth for GPU-spend endpoints
Corrected after review — I had first mis-filed this key as a harmless
"Render-side auth value." It is not. `app.py` (the root Flask service) exposes
`/api/mobile/submit-job`, `/api/mobile/upload-url`, `/api/mobile/job-status`,
`/api/mobile/retry-job` and `/api/mobile/reel-url`, all guarded only by
`mobile_auth_required` (`app.py:53-60`), which compares `Authorization: Bearer …`
against a single shared `MOBILE_API_KEY`. The app sends that bearer from
`lib/pipeline.ts:44` using `EXPO_PUBLIC_PIPELINE_API_KEY`, which Expo inlines
into every client bundle — so the "secret" is extractable from the shipped IPA
with `strings`. `clippar_app/CLAUDE.md:98-100` already lists this as a known
landmine ("Secret keys ship in the client … Move behind an Edge Function proxy").

> **Attacker:** anyone who extracts the pipeline key from the app bundle (no
> account needed).
> **They send:** `POST /api/mobile/submit-job` with that bearer and any
> `round_id`, to the Render pipeline host.
> **They get:** arbitrary pipeline jobs, which `worker.py:26-47` dispatches to
> the Modal GPU pipeline → externally-triggered GPU spend and queue exhaustion
> (a cost-DoS), plus reach to the other `/api/mobile/*` endpoints.

**Severity is deployment-dependent, and I can't see infra.** The current
shipping app's live processing path is the `process-round` **edge function** →
Modal (JWT- and ownership-checked, with `pipeline_secret`), and the July 29 audit
noted `lib/pipeline.ts` has no call sites in the shipping app and its Modal
credentials pointed at a since-deleted project — evidence the `app.py` Render
path is legacy. **If that Render service is not deployed, there is no live attack
surface today**; the moment it is (or is redeployed), the bundled key protects
nothing. Either way it should not be called clean.

**Left as-is, reported** — the fix is architectural (put these endpoints behind
an authenticated edge-function proxy or scope them per-user server-side, and
retire the bundled key), exactly the class of change the audit rules say to
describe rather than make unattended. **Recommendation:** confirm whether the
Render `app.py` service is deployed; if yes, treat `MOBILE_API_KEY` as public and
rotate/retire it and gate those endpoints on the Supabase JWT; if it's dead,
delete the Render service and `lib/pipeline.ts` and drop `EXPO_PUBLIC_PIPELINE_API_KEY`
from the build.

### Low — `sync-courses` admits admins by email match, not just user id
`clippar_app/supabase/functions/sync-courses/index.ts:44-51`. `isAdminUser()`
returns true if either `admin_users.user_id = <jwt uid>` **or**
`admin_users.email = <jwt email>`.

> **Attacker:** someone who can obtain a session whose `user.email` equals an
> admin's email.
> **They get:** the admin-gated actions `approve_suggestion` and `sync_region`,
> letting them mutate the **shared course catalog** (not tenant data, not
> payments).
> **Precondition:** they must get a JWT carrying an admin's email. If Supabase
> email confirmation is enforced they cannot (they'd need the admin's inbox); if
> confirmation is ever disabled, or an admin's email is re-registerable, the
> email branch becomes a real escalation.

**Left as-is, reported.** The safe fix is to drop the email branch and match on
`user_id` only — but I can't see the `admin_users` table, and if its rows are
keyed by email rather than user_id, removing the branch locks out every admin.
This is an authorization path; per the audit rules I won't change it unattended.
**Recommendation:** decide deliberately — either (a) enforce mandatory email
confirmation and keep the branch, or (b) backfill `admin_users.user_id` and drop
the email match. Severity is capped at "catalog tampering", hence Low.

### Low — dormant `redeem-code` edge function is deployable dead code
`clippar_app/supabase/functions/redeem-code/` and its migration
`supabase/retired/018_redemption_codes.sql`. The function was superseded on
2026-08-04 by App Store Offer Codes; the migration is retired (not in the push
path), the tables don't exist in prod, and `app/profile/redeem.tsx` uses the
StoreKit redemption sheet — there is no client call to the function. Its design
was sound (getUser-verified user only, CSPRNG codes, fail-closed rate buckets,
atomic single-UPDATE consumption). Residual risk is only that someone deploys it
by mistake; it would 500 on the missing table.

**Left as-is, reported.** **Recommendation:** delete the `redeem-code` directory,
the retired migration, and the now-unused `REDEMPTION_CODE_PEPPER` secret once
Offer Codes are confirmed working in production. Kept out of this branch to
avoid deleting a whole function unattended.

### Informational — stale rate-limit comments could be "fixed" backwards
`search-courses/index.ts:143` and `get-shared-reel/index.ts:77` still say the
client IP keys on the *last* `X-Forwarded-For` entry. The shared helper
`_shared/rateLimit.ts` was correctly changed to prefer `cf-connecting-ip` then
the *first* entry. The code is right; the comments are stale and describe the
opposite, so a future reader could "correct" the code into a per-request-bucket
bypass. Worth a one-line comment fix next time these files are touched.

---

## 3. Checked and found clean (so tomorrow doesn't repeat it)

**Committed secrets** — the **working tree and `main` are clean**, but full
history is **not** (see 1a: a live Resend key on an unmerged pushed branch —
this is why the scope note above matters). Beyond that one docx: no
`sk_live`/`whsec_`/`AIzaSy`/`service_role…eyJ`/PEM blocks anywhere in tree or
full history; the `_shared/apple.ts` hits are documentation/regex; `.p8` strings
in tests are locally-generated fixtures. Supabase JWTs in `r.html` decode to
`role:anon` (public by design); the Sentry DSN is a public client DSN. All
tracked `.env.*.example` files contain placeholders only (`YOUR_…`,
`pk_test_…`, `appl_YOUR_…`). No real Stripe/RevenueCat/Shopify/Slack/GitHub/AWS
tokens. `.gitignore` correctly covers `.env`, `service_account.json`,
`*.p8/.p12/.key`, `*.vsix`, `Mailing key.docx`, SQLite WAL files — but those
rules postdate the `5aa2835` commit, so they do nothing for the blob already
pushed on that branch.

**Client bundle secrets** — the keys read from `EXPO_PUBLIC_*` /
`constants/config.ts` are the Supabase anon key, Stripe **publishable** key, and
RevenueCat **public** SDK key, all public by design; no service-role or secret
key is inlined. The GolfCourseAPI key was moved server-side. **Exception:**
`EXPO_PUBLIC_PIPELINE_API_KEY` is *not* harmless — it is the sole auth for the
`app.py` GPU-spend endpoints; see the Medium finding in §2. Do not treat it as
clean.

**Row Level Security** — no new tables or migrations since 019, so no new
unpoliced surface. The full table-by-table sweep in `TENANT_ISOLATION_2026-07-31.md`
still holds; I re-confirmed 019 is present in the tree and its `profiles` INSERT
re-grant, `shots` round-ownership WITH CHECK, `clips` storage policies, and
`public=false` force are all as written. (Deployment of 019 is the open question —
see 1a.)

**Edge functions (all 12)** — every user-facing entry point verifies the JWT
server-side with `supabase.auth.getUser(token)` and acts only on the verified id;
none trusts a body-supplied user id. `stripe-webhook` does real
`constructEvent` signature verification and returns a fixed `'Webhook Error'`
string (the July-29 err.message leak is fixed and still fixed).
`revenuecat-webhook` uses constant-time shared-secret comparison, refuses sandbox
events, and requires an allowlisted product id before writing lifetime access.
`get-shared-reel` signs a key **derived from the round id**, so `reel_url` cannot
influence what is signed (the signing-oracle fix holds). `process-round` checks
ownership, claims atomically via the service-role-only `dispatch_claimed_at`, and
no longer sends the service-role key to Modal. All client-callable functions are
rate-limited; no raw `err.message` reaches any caller.

**Injection** — Supabase `ilike('name', '%'+query+'%')` sends the value as a
bound parameter (not string-built into `.or()`); no concatenated `.or()`/`.filter()`
filters exist. Python workers: `db.py` and `modal_detector.py` interpolate only
code-controlled column names, with all values passed as `%s` parameters. The
public waitlist `clippar-web/api/submit.py` is fully parameterized, body-capped
(4 KB), format/length-validated, honeypotted, and rate-limited fail-closed
per-IP and globally.

**Insecure storage** — the Supabase session (with the long-lived refresh token)
is stored only in `expo-secure-store` (Keychain, `…ThisDeviceOnly`) on native;
the in-memory mirror never persists to disk, and legacy AsyncStorage sessions are
purged on launch. No token or secret is written to AsyncStorage.

**Storefront / web** — `clippar-web` ships a strict CSP (`default-src 'self'`,
`object-src 'none'`, `frame-ancestors 'none'`), plus HSTS, `X-Frame-Options:
DENY`, `nosniff`, a locked-down `Permissions-Policy`, and `Referrer-Policy`. The
public shared-reel viewer `public/r.html` renders every attacker-controlled field
via `textContent` / `createElement` — never `innerHTML` — and encodes the share
token into the request. No XSS sink, no token exposed in theme assets.

**Dependencies** — `npm audit --omit=dev` flags high-severity advisories in
`@babel/core`, `@xmldom/xmldom`, `brace-expansion`, `fast-uri`, `image-size`,
`metro`, `@expo/metro`. **All are build-time bundler/toolchain packages** — none
ships in the app bundle or runs in the edge-function (Deno) runtime, and every
advisory requires feeding malicious input to the bundler at build time (i.e. the
attacker already controls the build). None is runtime-reachable by an external
attacker, so none is a real finding. Worth a routine `npm audit fix` for hygiene
when convenient, but not security-urgent.

---

## Carry-forward summary (for tomorrow)

- [ ] **1a** ROTATE the exposed Resend key + second token NOW; purge the docx (and `kickbacks.vsix`) from the pushed branch; extend CI scan to all branches
- [ ] **1b** verify migration 019 applied to prod — gates a live P0 if not
- [ ] **1c** tighten `avatars` bucket storage policy (open since 2026-07-31)
- [ ] **1d** confirm GolfCourseAPI rotation stuck; purge old key from history
- [ ] **2** confirm whether the `app.py` Render pipeline is deployed; if so, retire/rotate the bundled `EXPO_PUBLIC_PIPELINE_API_KEY` and JWT-gate `/api/mobile/*`
- [ ] **2** decide on `sync-courses` email-admin match
- [ ] **2** delete dormant `redeem-code` function + pepper secret
- [ ] stale XFF comments in `search-courses` / `get-shared-reel`

Still-open items inherited from the July 29 audit that are product/undeployed and
not re-verified here: `resetToStart` clip-unlink undo, Sentry `beforeBreadcrumb`,
unscoped temp-export sweep, `delete-account` reel-cleanup (leaves orphaned reel
bytes at `clips/reels/<roundId>.mp4`), and EAS Update code signing. None is an
external-attacker disclosure path; they remain as noted in that report.
