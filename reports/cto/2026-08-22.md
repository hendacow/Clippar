# CTO review — 2026-08-22

Combined code review + security audit of the last 36 hours. **Quiet window:** the only
commit was the merge of yesterday's own review PR (#139, merged by Henry 22 Aug 15:30
AEST). Per the routine's standing rule, the time went to verifying prior findings
instead of inventing new work.

## New findings

None. The only new code on `main` is #139's two wrapped-round fixes, which were
re-read here and re-verified: the `buildHoleDataFromPars` wrap formula is byte-for-byte
the `liveHoleNumbers` formula, the lockstep test pins the two together for all four
round shapes, and the Prev-button disable now derives from `previousHoleTarget` so
state and behaviour cannot disagree. `npm run verify` on `main`'s tip (`7af2227`):
**519/519 tests pass, tsc clean.** No secrets in the #139 diff.

## Prior findings — verification status

| Finding (source) | Status today |
|---|---|
| Wrapped rounds scored front nine at par 4 (2026-08-21, HIGH) | **Closed** — fix merged in #139, verified on main |
| Prev Hole dead for half a wrapped round (2026-08-21, MED) | **Closed** — merged in #139, verified on main |
| GolfCourseAPI key `JL2H…` in git history (2026-08-21, MED) | **Still open — needs Henry** at the GolfCourseAPI.com dashboard. Nothing in-repo can close it. |
| `EXPO_PUBLIC_PIPELINE_API_KEY` in client bundle (2026-08-21, LOW) | **Still open by design** — legacy pipeline path's fate is a product call. Unchanged since yesterday. |
| stripe-webhook error-echo leak (2026-07-29) | **Re-verified closed** — signature failures log server-side only; callers get a fixed `'Webhook Error'` string (`stripe-webhook/index.ts:120-138`). |
| Clips bucket bucket-wide access (2026-07-31 P0) | **Re-verified closed in-tree** — 019 owner-scopes SELECT/UPDATE/DELETE (including the reel keyspace), 017 owner-scopes + quota-gates INSERT, 019 §4d forces the bucket private. |
| Auth tokens in AsyncStorage | **Re-verified closed** — `lib/supabase.ts` uses expo-secure-store (`WHEN_UNLOCKED_THIS_DEVICE_ONLY`), with a deliberately-documented ephemeral fallback. |
| `avatars` policies / prod `clips` public flag (dashboard state) | **Structurally unverifiable from the repo** — still waiting on migration 019 §6's dump at the next Supabase deploy. |

## Fixed today

One documentation bug, found during verification: `clippar_app/CLAUDE.md`'s "Known
security landmines" still asserted **"Storage RLS is NOT owner-scoped"** — a P0 that
migrations 011/017/019 closed weeks ago. A stale landmine is worse than none: it tells
every future session the worst finding we ever had is still open, and invites a
"fix" that would re-litigate 019's carefully-argued policies. Rewritten to state the
current truth and the one thing still unprovable from the repo (dashboard state).
The other two landmine bullets were checked and stand: client-bundled API keys (true,
tracked), no soft-delete (true — `deleted_at` appears nowhere in migrations).

## Deliberately not touched

- `ShotDetectorModule.swift` — no commit in scope touched it; standing rule against
  speculative changes to the Swift/concurrency/export path holds.
- The legacy pipeline path (`lib/r2.ts`, `lib/pipeline.ts`) — retiring it or proxying
  the key is Henry's product call, unchanged from yesterday.

## Needs Henry (same single item as yesterday)

1. **Confirm the old GolfCourseAPI key (`JL2H…`) is disabled** at GolfCourseAPI.com.
   Two minutes; the only open credential item. It has now survived two consecutive
   reports — worth doing today.

## Verification

`npm run verify` on `main` (`7af2227`): 519 tests pass, `tsc --noEmit` clean.
