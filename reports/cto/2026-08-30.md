# CTO review — 2026-08-30

Combined code review and security audit, one pass over one diff.

**Scope.** `main` has not moved since PR #150 (29 Aug). All the change in the last 36
hours is on `feat/onboarding-fast-value` — 16 commits, ~3,100 added lines, none of it
merged and none of it previously reviewed. That branch is the diff this report covers:
`git diff origin/main...origin/feat/onboarding-fast-value`.

**Checkout.** `git fetch --unshallow` first (292 commits, not 50). Without it the secret
scan refuses to run and the history half of the audit silently does not happen.

**Baseline.** `npm run verify` on the branch before any change: **593/593, exit 0**.
After the fixes below: **627/627, exit 0**.  
**Eight review rounds ran on the PR.** Findings 22-25 came from the sixth, 26-29 from the seventh and 30-31 from the eighth; of those ten, five were taken.

---

## Severity summary

| # | Finding | Severity | Status |
|---|---|---|---|
| 1 | Tutorial sweep deletes any round the user names "Tutorial round" | **High** | Fixed |
| 2 | Concurrent clip deletes can lose a bin entry → unrecoverable clip | Medium | Fixed |
| 3 | Failed bin write leaves a clip deleted with no recovery record | Medium | Fixed |
| 4 | Recently-deleted screen labels practice clips "Hole 8", not "7 Iron" | Low | Reported |
| 5 | Legacy Flask service: three hardcoded fallback credentials, incl. the session-signing key | Medium if ever deployed; not deployed | Carried, described not fixed |
| 6 | `sync-courses` grants admin by email as well as user id | Low (inert) | Carried, unchanged |
| 7 | Clip bin is device-wide: one account can destroy another's footage | Medium | Fixed (review) |
| 8 | Binned videos survive "remove my videos from this phone" | Medium | Fixed (review) |
| 9 | Tutorial id registry is device-wide: same cross-account delete, bounded | Low | Fixed (review) |
| 10 | This report quoted a fallback credential instead of citing its line | Low | Fixed (review) |
| 11 | **Practice sessions: one account could watch another's swing videos** | **High** | Fixed |
| 12 | Account deletion wipes `clips/` wholesale, destroying another account's videos | **High** | **Out of diff — reported, NOT fixed** |
| 13 | Training live capture accepted any `roundId` from the URL (deep-linkable) | Medium | Fixed |
| 14 | My own legacy-bin drain destroyed other accounts' files | Medium | Fixed |
| 15 | `deleteClipToBin` scoped the bin key but not the clip it deleted | Medium | Fixed |
| 16 | Bin entries fed dynamic SQL identifiers with no shape validation | Low | Fixed |
| 17 | My `deleteClipToBin` gate validated `roundId` while the delete keyed on `clipId` | Medium | Fixed |
| 18 | Training ownership rested on one input another account's sign-in can reassign | Medium | Fixed |
| 19 | Tutorial sweep still fail-open on a null row → unscoped delete | Medium | Fixed |
| 20 | My wipe blanket-deleted the device-wide legacy bin key | Medium | Fixed |
| 21 | `binKey()` may inherit a stale cached user id | Medium, contested | **Escalated — reviewers disagree, needs a device** |
| 22 | My ownership verdict went stale across a `roundId` change, re-arming the camera | Low | Fixed |
| 23 | **Every destructive gate rests on `local_rounds.user_id`, which the NULL-owner backfill lets a later account claim** | **Medium** | **Escalated — core claim mechanism, not mine to change** |
| 24 | `tutorial.created_round_ids` / `training.sessions.v1` survive "erased forever" | Low | **Escalated — the obvious fix repeats a mistake I made twice** |
| 25 | The SQL identifier guard sits in the caller, not at the sink; `restoreLocalClip`'s docstring is now false | Low | **Escalated — shared primitive** |
| 26 | My comment claimed the camera prompt was behind the ownership check. It was not. | Low | Fixed |
| 27 | `isValidEntry` authorised on `e.roundId` while `purgeEntry` destroyed via `e.row` | Low | Fixed |
| 28 | **My own fix for 20 left the departed account's clip metadata — GPS included — permanently uncollectable** | Medium | **Escalated — caused by this PR** |
| 29 | Finding 23, re-rated **HIGH**: the adopted round is reachable through the Resume card | **High** | **Escalated — same as 23** |
| 30 | My ownership gate had a one-frame hole: the reset ran an effect too late | Medium | Fixed |
| 31 | **`deleteClipToBin`'s bin key and ownership gate were decided by two different session resolutions** | Medium | Fixed |

**Nothing needs a credential rotated** (with one conditional: if the legacy Flask service
at 5 was ever reachable, all three of its fallback values should be).

**Correction to this report's own first draft.** It said "no new security finding". That
was badly wrong, and the gap is the most useful thing in this report. Findings 7-10 came
out of the automated review on PR #152 — after I had written that line — and 7 is a
cross-account data-destruction path in the diff I had just reviewed and called clean.
Finding 11, the most severe of the night, came from taking that lesson and applying it to
the rest of the branch myself: one account could **watch another account's swing videos**.

So the honest summary is that my first pass caught the correctness bugs and missed the
security ones entirely, in a direction the codebase already has a documented defence
against. Detail in "What the review caught that I did not", below. The line is corrected
rather than softened.

---

## Part 1 — Correctness

### 1. HIGH — a user-typed course name could delete that round, permanently

`lib/tutorialRound.ts`. `sweepTutorialRounds()` selected rounds to destroy by course
name:

```ts
const orphans = await getOrphanedRounds().catch(() => []);
for (const o of orphans) if (o.course_name === TUTORIAL_COURSE_NAME) candidates.add(o.id);
```

`TUTORIAL_COURSE_NAME` is the string `'Tutorial round'`, and `course_name` is free text —
`app/(tabs)/record.tsx:1535` binds a `TextInput` straight to it via
`onChangeText={setCourseName}`.

**The failure, concretely.** A golfer types "Tutorial round" as the course on the
round-setup screen and plays it. `getOrphanedRounds()` returns it (it is `in_progress`
and owned by them), the name matches the sentinel, and the guard on the next line
(`if (row && row.course_name !== TUTORIAL_COURSE_NAME) continue`) passes it through
because the name genuinely does match. `deleteLocalRound(id)` then drops the local rows
and unlinks every clip file, and `deleteRound(id)` deletes the remote row. **The round
and every video in it are gone, on the phone and on the server. Nothing is written to
the recently-deleted bin and there is no undo.**

It runs on every authed app start — `app/_layout.tsx:114`, `if (!active) void
sweepTutorialRounds()` — with no feature-flag gate. So although v3 onboarding is still
off by default, the sweep that serves it is live for every user the moment this branch
merges.

Likelihood is low: it needs that exact string. Consequence is total and irreversible, and
the trigger is entirely in the user's hands.

**Fix.** A sentinel the user can type is not a sentinel. Candidates now come from an id
registry — `tutorial.created_round_ids`, written by `createTutorialRound` immediately
after the round is created — so only rounds this app created as tutorial rounds are ever
eligible. The `getOrphanedRounds()` scan is gone. The course-name comparison is kept
underneath as a second gate rather than as the only one, and swept ids are pruned so the
registry does not grow for the life of the install.

Cost of the change: if the app dies in the window between `createRound` returning and the
registry write completing, that tutorial round survives as clutter in the user's history
instead of being collected. Leaving clutter is the right side of this trade.

### 2. MEDIUM — two overlapping deletes could lose a clip the UI promised was recoverable

`lib/clipBin.ts`. All three mutations were read-modify-write over one `local_settings`
row. `getSetting`/`setSetting` are a bare `SELECT` and a bare `INSERT OR REPLACE` — no
transaction, no compare-and-swap, no row lock.

**The failure, concretely.** `deleteClipToBin` removes the clip row from SQLite *first*
and records the recovery entry *second*. Two of those overlapping: both read the same bin
array, both write; the second write was computed from a snapshot taken before the first
landed, so the first clip's entry is dropped. That clip is now deleted from SQLite with
nothing to restore it from — while the confirmation dialog has just told the golfer "You
can put it back from Profile → Recently deleted."

`useEditorState.removeClip` fires this deliberately without awaiting ("to keep the tap
responsive"), which is what makes overlap possible at all.

Honest on likelihood: both delete affordances in the editor confirm through an
`Alert` first, and there is no bulk-delete path, so consecutive taps are realistically
hundreds of milliseconds apart against a window of a few DB round-trips. This is a
narrow race, not a routine one. It is fixed because it is cheap to fix and the promise
in the dialog should hold unconditionally.

**Fix.** New `lib/serialQueue.ts` — a promise-chain queue that makes "read, decide,
write" atomic with respect to other work on the same instance. All three bin mutations
run through it. A rejected job settles the tail so one failure cannot wedge the queue.

The test for this is behavioural, not a source grep: it drives an unserialised
read-modify-write against a fake store and asserts that it *does* lose an entry, then
asserts the queued version does not.

### 3. MEDIUM — a failed bin write left the clip unrecoverable

Same function, different path. `deleteLocalClip` had already removed the row when
`writeBin` ran; if that write threw (storage pressure being the obvious cause), the row
was gone and no bin entry existed. `removeClip`'s `.catch(() => {})` swallowed it, so the
golfer saw the clip disappear and nothing else.

**Fix.** The bin write is wrapped; on failure the row is restored with
`restoreLocalClip(row)` and the error is rethrown. The clip then reappears on the
editor's next focus reload — the same visible outcome as the bug this whole feature
replaced, and strictly better than losing the shot.

### 4. LOW — practice clips are labelled as holes in Recently deleted (not fixed)

`app/profile/deleted-clips.tsx` prints `Hole {hole_number} · shot {shot_number}`. In a
training session `hole_number` is a club index (`lib/training.ts` `CLUBS`), so a deleted
7-iron shot shows as "Hole 8 · shot 3". The editor is careful about this everywhere
else — `trainingHoleLabel()` exists for exactly this — but the bin screen predates the
mapping.

Not fixed: the entry carries `roundId`, so the screen would need an async lookup against
the training registry per row, and I would rather Henry decide whether that is worth the
extra read than have me add one on my own judgement. Cosmetic, no data at risk.

### Verified and NOT a finding

Worth recording, because each of these looked like one:

- **The +300ms impact calibration is complete and consistent.** `calibrateImpact` is
  applied at both native entry points that produce an estimate — `detectAndTrimVideo`
  (line ~350) and `processVideo` (line ~555) — so `detectSwing` and `detectAndTrim`
  cannot disagree. `detectBallLaunch` takes `impactTimeMs` as a parameter from JS, so it
  inherits the calibrated value. Selection, classification and confidence all still run
  on the raw pick, as Henry asked. The clamp to clip duration is the pre-existing
  behaviour of the trim window, not new.
- **`moveClipToHole` really does create a missing target section**, so offering all 15
  clubs in training mode (including empty ones) cannot strand a clip. Read the reducer at
  `hooks/useEditorState.ts:429-450` rather than trusting the comment.
- **`importShotsToSession` does not delete the user's originals.** `persistAsset` only
  *moves* when the source is our own cache (`isPurgeableAppPath`); a Photos `localUri` is
  copied. Matches what `app/round/import.tsx:671` already does.
- **No unique constraint on `(round_id, hole_number, shot_number)`**, so the import's
  shot numbering restarting after a delete cannot fail an insert. Duplicate shot numbers
  are cosmetic.

---

## Part 2 — Security

**Nothing new.** The branch touches no migration, no edge function and no auth path.

1. **Committed secrets — clean.** `scripts/secret-scan.sh` over the full unshallowed
   history: `secret scan CLEAN`, exit 0. Tracked dotenv files, live credential shapes,
   opaque binaries, non-production endpoints, unpinned remote imports — all clear.
   **No credential needs rotating.**
2. **Row Level Security — unchanged and intact from source.** No new migration (highest
   is still `019_tenant_isolation.sql`). 16 tables carry `ENABLE ROW LEVEL SECURITY`, and
   no policy in any migration uses `USING (true)`, `WITH CHECK (true)` or `TO public`.
   Per `clippar_app/CLAUDE.md` this is a source read, not a statement about production —
   a checkout cannot tell you what is deployed.
3. **Edge functions.** Two of twelve are intentionally public and both are hardened:
   `stripe-webhook` verifies `constructEvent` against `STRIPE_WEBHOOK_SECRET` before
   doing anything, and `get-shared-reel` requires a 128-bit share token plus
   `is_published = true`, is rate-limited before the token is even read, and returns a
   uniform "not available" so it cannot be used to probe which tokens exist. The other
   ten resolve identity from the verified JWT via `supabase.auth.getUser(token)`. None
   derives identity from a client-supplied user id.
4. **Client-side secrets / injection.** No change on this branch. The existing test
   "SQL still uses bound parameters, never string interpolation" passes.

### Carried findings, re-verified rather than taken on trust

- **Legacy Flask service — worse than this report first stated, and deliberately
  under-described.** The retired Flask service at the repo root takes three credentials
  from the environment with hardcoded literal fallbacks — one of them the Flask
  session-signing key, which matters more than the admin password does. Last touched
  6 April 2026, not referenced by any workflow, but the root `Dockerfile` builds and
  runs it, so an unconfigured deploy would come up using published values.

  **The details stop there on purpose.** I confirmed via the API that this repository is
  public (`"visibility": "public"`), and an earlier draft of this section spelled out the
  exact lines, the exploitation chain and the deployment path. Redacting the literal
  values — which is all the first correction did — bought nothing, because they sit a few
  lines away in the same public tree. What actually raised the exposure was the
  *synthesis*, and that is now removed. Henry can read the three lines; a passer-by gains
  less from the finding than they did an hour ago.

  **This is the real constraint, and it should be named:** an audit report committed to a
  public repository cannot describe an unfixed weakness in that same repository without
  advertising it. Redaction does not resolve that — only closing the finding does. So
  this is escalated rather than documented.

  **Not fixed here, deliberately.** The correct fix is to fail closed — take all three
  from the environment with no default, so an unconfigured deploy refuses to boot — plus
  a constant-time password comparison. That is authentication code, which my standing
  instruction says I do not rewrite on my own judgement, and it belongs to a service
  unrelated to this PR's diff. If the service is genuinely dead, deleting it and its
  Dockerfile closes the finding outright and is the cleaner option. **Henry's call, and
  it is now on his list rather than buried here.** If that host was ever reachable,
  rotate all three values.
- **`sync-courses` admin-by-email.** `isAdminUser(user.id, user.email)` checks
  `admin_users` by `user_id` and, failing that, by `email` — so an account's privilege
  follows its email address. Exploiting it requires passing Supabase's confirmation on
  the new address, i.e. controlling the admin's inbox, at which point the account is
  already lost. Genuinely inert today; the defect is that it widens "admin" from an
  account id to an inbox. Open and undecided since 20 Aug.

---

## What the review caught that I did not

Worth recording plainly, because it is the most useful thing in this report. Four
findings came from the automated reviewers on PR #152, and one of them is a real
cross-account data-destruction path sitting in a diff I had already reviewed and
described as clean.

### 7. The clip bin was device-wide (Medium — fixed, `cbad128`)

`BIN_KEY = 'clips.bin.v1'` is one `local_settings` row, and `local_settings` is
`(key TEXT PRIMARY KEY, value TEXT NOT NULL)` — no owner column. `listBinnedClips()`
applied no ownership filter, and the Recently-deleted screen calls it with no round id.

**The failure:** one `clippar.db` is shared by every account that signs in on a handset,
and sign-out deliberately does not wipe it. User A deletes shots; the bin keeps the rows
*and the video files*. A signs out, B signs in. B opens Profile → Recently deleted and
sees A's shots. "Delete for good" unlinks A's video files; "Put back" reinserts A's row
under B's session.

What makes this bad rather than theoretical: `lib/localScope.ts` exists *specifically* to
close this exact scenario, and documents it in its header — A leaves a round in progress,
signs out, hands the phone over, and B's `getOrphanedRounds()` used to hydrate A's
footage into B's session. The bin walked straight past a defence this codebase had
already built and paid for.

**Fixed** by keying the bin per account — `clips.bin.v1.<userId>`, the same convention
`pro.status_cache.<userId>` already established — failing closed when no session
resolves, and refusing the delete outright rather than removing a row it cannot record.

### 8. Binned videos survived the explicit "remove my videos" (Medium — fixed)

Same root cause. `removeLocalMediaForCurrentUser` walks rounds through
`deleteLocalRound`, but a binned clip no longer *has* a `local_clips` row — so up to 30
videos survived the one action whose entire purpose is removing them, and survived
sign-out. `clearLocalDatabase` likewise dropped only the Pro cache from `local_settings`.
Both now handle the bin.

### 9. The tutorial id registry had the same shape (Low — fixed, `a634fb8`)

My own fix for finding 1 introduced this. `getLocalRound` is scoped and fails closed, so
under account B a round of A's reads as `null` — which my "a missing local row is
expected" comment waves through — while `deleteLocalRound` is *not* scoped and deletes by
`round_id` alone. Entries now carry their owner; only the signed-in account's are
candidates; other accounts' entries are preserved so their rounds are still collected
later. Bounded impact (tutorial rounds hold bundled demo clips), fixed because this PR is
what made the registry the sole record.

### 10. This report quoted a credential (Low — fixed)

The first draft reproduced `app.py`'s `ADMIN_PASSWORD` fallback verbatim, in a document
written to be circulated, while noting in the same section that the repo is public. The
value was already in the tree so nothing new leaked, but it was a second copy for no
gain — `scripts/secret-scan.sh` argues exactly this about its own output and I failed to
apply it to my own prose. Redacted to a line reference, and
`tests/reportsCarryNoSecrets.test.ts` now derives the literals *from `app.py`* and
asserts no report contains them.

The same finding surfaced the `app.py:25` hardcoded Flask `secret_key`, which I had
missed and which is worse than the password: a known signing key means forging the admin
cookie directly, so the password never matters. Finding 5 is re-rated above.

### The lesson

The review found these in the same file I had just reviewed. My pass was focused on the
*new* code's own logic — races, rollback, retry — and I checked account scoping nowhere,
even though `localScope.ts` is a documented, deliberate invariant in this codebase and
every new `local_settings` key is a chance to break it. **A new `local_settings` key is a
scoping decision.** When the diff adds one, ask who owns it before asking whether it
works.

### Findings 13-16, and where I stopped

Four review rounds ran on this PR, and **every one found something in the previous
round's fix.** That is worth recording as honestly as the original miss:

- **13 (Medium, fixed).** I gated the training reads and the import write and left live
  capture open. `app/training/record.tsx` took `roundId` from the URL, `app.config.js`
  registers a URL scheme so the route is externally reachable, and `useCamera`'s save
  path is a bare `saveLocalClip({ round_id })` with no ownership predicate. Worse, my own
  read-gating made the failure *silent*: the shot counter reads zero, so it looks like a
  fresh session rather than an error. This is the same "one of two routes protected"
  mistake I had criticised in finding 11, made again one file over.
- **14 (Medium, fixed).** My legacy-bin drain from finding 7's fix purged the *whole*
  device-wide list, on my assertion that its entries "cannot be attributed to an
  account" — which would have unlinked the other account's videos, recreating the bug it
  was fixing. They can be attributed: only the clip row was deleted, the round row
  survives, and `getLocalRound` is scoped. **Second time in this PR I reasoned about
  ownership instead of verifying it.**
- **15 (Medium, fixed).** `deleteClipToBin` scoped the bin key but never checked the clip
  was ours. `deleteLocalClip` is `DELETE ... WHERE id = ?` with no ownership predicate
  and ids are small integers, so "callers gate upstream" was a property of today's call
  sites, not the function.
- **16 (Low, fixed).** `restoreLocalClip` interpolates `Object.keys(row)` into SQL
  unescaped, justified by a docstring saying the keys come from the database. This
  feature broke that invariant by round-tripping the row through JSON, and nothing
  recorded it. Validated at the boundary — by identifier *shape*, not an allow-list,
  because `local_clips` has 25 columns added by `ALTER TABLE` and a list would drift and
  silently refuse valid restores.

### Findings 17-21, and the pattern that decided when to stop

I announced after round 4 that I would stop, then reversed it once — because round 5
found that the gate I had just shipped in `deleteClipToBin` **validated a parameter that
did not constrain the delete**: it checked `roundId` while the destructive call keyed on
`clipId`, two independent arguments with nothing binding them. Three reviewers found it
separately. A gate that reads as protection and provides none is worse than no gate, so
that one was worth reversing for. Fixed by reading the round off the clip.

The same round found two more of mine (18: training ownership resting on
`local_rounds.user_id`, which another account's sign-in can reassign through the
NULL-owner backfill; 19: the sweep still falling open on a null row) and one outright
self-contradiction (20: I partitioned the device-wide legacy bin key by owner in
`clipBin` and then blanket-deleted the same key in `storage`, in the same commit).

**That is the honest shape of this session.** Five rounds, and across them I made the
same class of mistake four separate times: deciding ownership from something the caller
or another account controls, rather than from the thing being protected. Each individual
fix was right; the reasoning that produced them kept reaching for the convenient input.

**21 is declined, not fixed** — the only finding on this PR I have turned down. Two
reviewers reached opposite conclusions on whether `binKey()` can inherit a stale cached
user id. The stricter reading is the better argument, but the fix changes a shared
resolver's contract so that every caller fails closed during a routine Keychain-busy
window, and settling which cost is worse needs a device. Both readings are on Henry's
list rather than guessed at unattended after five rounds.

Everything still open is under "Needs Henry" — item 3 is the one that matters, because
unlike everything fixed here it is **already live**.

### 11. Applying that lesson found the worst one — practice footage (High — fixed, `2013957`)

The branch adds three device-wide `local_settings` keys, not one. Checking the others
against the same question found this, and it is more severe than the bin finding that
prompted the check: it exposes the **footage itself**, not metadata plus a destroy
button.

`training.sessions.v1` is device-wide, and `getClipsForRound` is a bare
`SELECT * FROM local_clips WHERE round_id = ?` with no ownership predicate.

**The failure, verified link by link rather than inferred:** A does range practice on
their phone. A signs out, B signs in. B opens Practice — `listTrainingSessions()` returns
A's sessions from the shared registry, and `trainingShotCounts()` returns A's true
per-club counts, so B sees "Dr ×12 · 7i ×8" for a session they never recorded. B taps
**Watch** → `app/training/play.tsx:67` calls `listTrainingClips(roundId)` → unscoped read
→ **B watches A's swing videos.**

The revealing detail is that the *editor* route off that same screen
(`index.tsx:258`) was already safe: `useEditorState.loadFromLocal` gates on
`getLocalRound`, which is scoped and fails closed, so B gets an error state. The player
simply never had that gate. One of two routes off one screen was protected — which is
exactly the argument for putting the check at the data layer instead of in a screen.

**Fixed** by gating `listTrainingSessions`, `listTrainingClips`, `trainingShotCounts` and
`importShotsToSession` on `getLocalRound` returning non-null. Deciding ownership from the
database rather than from the settings blob also covers registry entries written before
the check existed, with no migration and no shape change.

Not introduced by this PR — it is the trainee-mode code from the branch under review —
but finding it is what this run is for.

### 12. HIGH — account deletion destroys the other account's videos (out of diff, NOT fixed)

Surfaced as a passing observation in the review and verified here, because it is the same
class again and it is worse than most of what is above.

`wipeLocalUserData` (account deletion) does two things in sequence. First
`clearLocalDatabase`, which is **carefully scoped per user** — its own comment explains
that account A deleting their account must not destroy account B's footage, and every one
of its deletes joins through `user_id`. Then, immediately after, `removeOwnedMediaDirectories()`:

```ts
const OWNED_MEDIA_DIRS = ['clips/', 'exports/'];
await FS.deleteAsync(`${root}${dir}`, { idempotent: true });
```

That is the **whole** `documentDirectory/clips/` tree, with no ownership filter at all.

**The failure:** shared handset, A and B have both used the app. A deletes their account.
A's rows go, correctly scoped. Then every video file on the device goes — including B's.
B's SQLite rows survive, now pointing at files that no longer exist, so B opens their
rounds to a library of broken clips. The scoping immediately above is undone by the line
immediately below it.

**Not fixed, deliberately, and this is the one I most want a second opinion on.** It is
outside this PR's diff, it is account-deletion semantics — irreversible by design — and
the minimal correct fix is not a one-liner: the per-row unlink inside `clearLocalDatabase`
already handles the departing user's own files, so the directory sweep exists to catch
*orphans*, and making it safe means deleting only files no surviving row references rather
than dropping the tree. That is a real change to a destructive path, and I would rather
Henry saw the reasoning than have me guess at it inside a review PR.

Filed to `BACKLOG.md` and raised on Henry's list.

### 30. My own ownership gate had a one-frame hole, and my own fix put it there (Medium — fixed, `d2f5dc7`)

The eighth review round found this, and it is worth recording because it is the same
mistake once more in a different disguise.

Round five added `setOwned(null)` at the top of the ownership effect, precisely to stop
the previous round's verdict surviving a deep link into the already-mounted record screen.
That reset lives in a `useEffect`, and **effects run after the render commits**. So when
expo-router updated `roundId` in place, there was one fully painted frame in which
`roundId` was the new, unverified id while `owned` still read `true` from the round
before it. In that frame the `useCamera` binding took the new round id and both render
gates passed.

**The failure:** B is recording in their own practice session. A page, QR code or another
app fires `clippar://training/record?roundId=<A's round>`. The params update in place, the
camera is re-bound to A's round for that commit, and a finalize resolving inside the
window calls `saveLocalClip` with `round_id` set to A's round — B's footage written into
A's session. Narrow, and it needs a save to land in the window rather than a human tap.

**Fixed.** The stored verdict now carries the id it was computed for, and `owned` is
derived during render rather than reset from an effect, so a new id invalidates the old
answer in the same commit that introduces it. That closes the window instead of shrinking
it. The three-state contract and both render gates are unchanged.

The pattern, for the eighth time in one day: **I keep deciding ownership from a value that
can change out from under the check.** Here it was not a caller's argument or another
account's column — it was my own state, one tick stale. The fix that finally holds is the
same shape every time: bind the answer to the thing it was computed about.

### 31. The bin key and the ownership gate were decided by two different resolutions (Medium — fixed, `6fc5ee6`)

The strongest finding of the night, and it settles half of the contested finding 21 from
the source rather than from a device.

I wrote a docstring saying *"every queued job resolves the key ONCE and threads it
through."* It was false. `deleteClipToBin` resolved the account twice: once through
`binKey()`, and once inside `getLocalRound`, which calls `currentScopeUserId` →
`sessionUserId` on its own. Threading the key closed the read/write half and left the
gate half open — and the gate is the half that authorises the destruction.

`sessionUserId()` returns the **cached** `lastKnownUserId` when `getSession()` errors and
the fresh id when it does not, so two calls straddling an account change are not
guaranteed to agree.

**Why that direction matters.** `lib/training.ts`'s `ownsRound` also resolves twice, but
both resolutions answer the *same* question and it requires both to pass — a divergence
there only makes the check stricter. In `deleteClipToBin` the two resolutions authorised
**different halves** of one operation: which bin to write, versus whether the clip may be
deleted. A divergence there makes it **looser**. Same primitive, opposite failure
direction. B's clip passes the gate, `deleteLocalClip` (unscoped, by id) removes it, and
the recovery entry — whole `local_clips` row, file URIs included — is filed into **A's**
bin, where A's Recently deleted can restore B's row or unlink B's videos for good.

**Fixed.** One inline resolution builds the key, and the gate closes against that same id
(`getLocalRound` only ever returns a row whose `user_id` equals its own resolution, so
comparing the two is what binds them). Strictly stricter: a mid-job account change now
refuses the delete rather than redirecting it. The user retries; nothing crosses.

**And the test that was watching this was watching the wrong thing.** It asserted one
`await binKey()` per mutation, with the message "must resolve the account once and thread
the key" — counting call sites, not resolutions. It passed for hours while the invariant
it named did not hold. Re-pointed at the property.

**Not carried into `drainLegacyBin`, deliberately.** The reviewer suggests the same
one-line binding on the drain's partition. It would be strictly safer there too, but it
moves entries from *purged* to *written back*, which makes finding 28's remanence worse —
a trade, in an irreversible path already under two escalations. That one goes to Henry
with the rest of the drain work rather than being decided here.

## Changes in this PR

Branch `cto/2026-08-30` off `feat/onboarding-fast-value` (the code being fixed exists
only there). **Nothing pushed to `main`.**

| File | Change |
|---|---|
| `lib/serialQueue.ts` | New. Promise-chain queue for read-modify-write over settings keys. |
| `lib/clipBin.ts` | Mutations serialised; failed write restores the row; bin keyed per account, failing closed; `purgeAllBinnedClips`. |
| `lib/tutorialRound.ts` | Sweep scoped to an owner-stamped id registry; orphan-by-name scan removed; ids retired only when both deletes succeed. |
| `lib/storage.ts` | Exported `currentSessionUserId`; `clearLocalDatabase` drops the departing account's bin key. |
| `lib/localWipe.ts` | "Remove my videos from this phone" now empties the bin too. |
| `lib/training.ts` | Session list, clip reads, shot counts and import all gate on round ownership. |
| `tests/serialQueue.test.ts` | New. Behavioural test of the queue + gates on clipBin and account scoping. |
| `tests/tutorialSweepScope.test.ts` | New. Gates on the sweep's candidate set, ownership and retry. |
| `tests/reportsCarryNoSecrets.test.ts` | New. Derives `app.py`'s fallback literals and asserts no report quotes them. |
| `tests/trainingOwnership.test.ts` | New. Gates on every training read/write, and pins that the player never bypasses them. |
| `lib/localWipe.ts` | "Remove my videos" purges the bin; the legacy device-wide bin is drained, not orphaned. |

Test count 593 → 627. Every assertion that encodes a fix was confirmed to fail against
the pre-fix code and pass after — thirty-five in total, checked in fourteen batches as the
fixes landed. Not vacuous.

**Deliberately not touched**, per standing instruction: authentication (including
`app.py`'s admin login, where the right fix is known and written up under finding 5),
Stripe / RevenueCat, and the Swift concurrency and video-export ordering. Finding 4 is
described rather than fixed.

---

## Needs Henry

1. **No credential to rotate, and nothing broken in front of customers** — none of this
   has shipped; it is all on the unmerged branch.
2. **Review PR #152 before merging `feat/onboarding-fast-value`.** Findings 1 and 11 are
   the ones that matter: a round deleted by its own name, and one account watching
   another's practice videos. Both were reachable from the branch as it stands.
3. **Finding 23/29 — the one I most want you to look at, and the deepest thing found today.
   Re-rated HIGH by a later reviewer, and I agree with the upgrade.**
   Every ownership gate this PR adds ultimately trusts `local_rounds.user_id`. But
   `saveLocalRound` writes a NULL owner when the session momentarily can't resolve and
   re-arms the one-time claim, so **the first account to do any scoped read adopts every
   unowned round on the handset.** On a shared phone that hands A's round to B, and then
   every gate I added passes for B — including the one that unlinks video files. I fixed
   this for practice sessions with a second stamp and left ordinary rounds, which are the
   bulk of what the bin holds, resting on the single reassignable input.

   Not fixed because the remedy changes the claim mechanism every scoped read in the app
   depends on, and getting it wrong makes rounds invisible to their own owner — worse than
   the bug. The reviewer's proposed shape (claim only ids this *process* wrote, cleared on
   sign-out) is in PR thread `#discussion_r3890174656` with a test to pin it.

   **Two things sharpen it beyond what I first wrote.** The reviewer traced the adopted
   round to the **Resume card**: B is shown "Unfinished round at <A's course>", taps
   Resume, and gets A's scores, GPS and raw swing videos — exportable to B's Photos and
   uploaded to Supabase under B's id. Not a latent gate weakness; the exact scenario
   `localScope.ts` says it closed. And `localScope.ts:69-70` asserts *"Everything written
   after the migration is stamped at insert time and is never claimable"* — `saveLocalRound`'s
   null-owner path breaks that invariant with nothing recording it. The pre-migration
   backfill is a documented trade; the post-migration re-arm is not.

   **There are two candidate fixes and I'd start with the narrower one.** Requiring
   `isRowVisible(row.user_id, me)` on top of the scoped read (`ownsRoundStrictly`) hardens
   the destructive paths without touching what every scoped read depends on. Changing the
   claim mechanism itself is the thorough fix and the riskier one. A recommendation, not a
   decision.
4. **Two contested/deferred items now need your call, both written up above:** finding 21
   (`binKey` staleness — two reviewers disagree, needs a device to settle) and the
   `restoreLocalClip` docstring, which still describes a trust boundary its caller now
   enforces instead of it. Neither is urgent.
5. **Finding 28 — and this one my own fix caused, so it is on me to flag loudly.** Removing
   the blanket delete of the legacy bin key (finding 20) stopped one account destroying
   another's recovery records — but left the departing account's entries permanently
   uncollectable, because `clearLocalDatabase` drops their rounds in the same pass and
   ownership then becomes undecidable forever. Those entries are whole clip rows including
   **GPS coordinates**, so "delete my account" leaves the deleted golfer's course locations
   on a handset that may be handed on. I traded an active destruction bug for a remanence
   bug; I still think that was the right direction, but it was a trade and I should have
   said so at the time. Patch is on PR thread `#discussion_r3890187543`.
6. **Account deletion destroys the other account's videos on a shared phone (finding 12).** Not fixed — out of this PR's scope and it is irreversible code. If two people have ever used one phone, one deleting their account wipes the other's footage. Want me to fix it in a separate PR?
7. **Decide what happens to the old Flask service at the repo root (finding 5).** It has
   three published fallback credentials and a Dockerfile that runs it, in a repo that is
   public. Two options, both quick: delete it and its Dockerfile if it is dead, or make it
   fail closed without the environment variables set. I have not done either — it is
   authentication code and a deployment decision. This is the item that has been carried
   longest with the least movement, and it is now the reason this report cannot describe
   its own findings freely.
8. Findings 24 and 25 are small and written up with patches — do them deliberately, not
   as another review round. **24 has a trap:** the obvious one-line fix blanket-deletes a
   device-wide key, which is the same mistake I made twice today; only the partitioned
   form is safe.
9. Two judgement calls in the PR, neither urgent. The tutorial-sweep fix changes *when* a
   tutorial round gets collected (clutter in a rare crash case, in exchange for never
   deleting a real round) — say if you would rather I narrowed it differently. And
   finding 4 (practice clips labelled "Hole 8" in Recently deleted) needs an extra
   database read per row: worth it, or leave it?
10. **Worth knowing, not acting on:** CI does not run the test gate on a PR between two
   feature branches — `ci.yml` only triggers into `main`. So #152's green checks do not
   include typecheck or tests; I ran them locally. Filed to the backlog with two options.

## Android

No new estimate this run; the branch is all JS above the native layer and changes nothing
about the detector. The 23 Aug figure stands — **4-6 months solo for parity, 3-4 if the
tracer is cut from v1** — with the 29 Aug correction that the reel exporter now has an
Android analogue (`media3 Transformer`) and the cost is concentrated on
`VNDetectTrajectoriesRequest`, which does not. That correction is still asserted from
knowing the Android media stack, not from a spike, and one day of spike remains the right
purchase before any date is committed.
